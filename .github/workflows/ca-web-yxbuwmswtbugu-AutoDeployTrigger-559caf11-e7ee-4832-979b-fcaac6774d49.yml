name: Trigger auto deployment for ca-web-yxbuwmswtbugu

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: [main]
    paths:
      - "**"
      - ".github/workflows/ca-web-yxbuwmswtbugu-AutoDeployTrigger-559caf11-e7ee-4832-979b-fcaac6774d49.yml"

  # Allow manual trigger
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CAWEBYXBUWMSWTBUGU_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CAWEBYXBUWMSWTBUGU_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CAWEBYXBUWMSWTBUGU_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          registryUrl: cryxbuwmswtbugu.azurecr.io
          registryUsername: ${{ secrets.CAWEBYXBUWMSWTBUGU_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAWEBYXBUWMSWTBUGU_REGISTRY_PASSWORD }}
          containerAppName: ca-web-yxbuwmswtbugu
          resourceGroup: rg-azure-search-openai-demo-csharp
          imageToBuild: cryxbuwmswtbugu.azurecr.io/ca-web-yxbuwmswtbugu:${{ github.sha }}
